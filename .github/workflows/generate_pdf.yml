name: Generate Outgoing Confirmation PDF

on:
  # Railway 서버로 대체됨 — 긴급 시 수동 실행용 백업
  # repository_dispatch(types: [generate-pdf])는 Railway로 이전
  workflow_dispatch:
    inputs:
      record_id:
        description: "출고확인서 Record ID"
        required: true

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Cache NanumFont
        id: cache-nanum
        uses: actions/cache@v4
        with:
          path: /usr/share/fonts/truetype/nanum
          key: nanum-font-v1

      - name: Install Korean font
        if: steps.cache-nanum.outputs.cache-hit != 'true'
        run: |
          sudo apt-get install -y fonts-nanum

      - name: Install dependencies
        run: pip install -r pdf/TMS_pdf_requirements.txt

      - name: Generate PDF
        env:
          AIRTABLE_PAT: ${{ secrets.AIRTABLE_API_KEY_TMS }}
          PDF_OUTPUT_DIR: /tmp
        run: |
          python pdf/출고확인서_tms.py --record-id "${{ github.event.inputs.record_id }}"
