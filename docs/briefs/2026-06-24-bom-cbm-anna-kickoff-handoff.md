# BOM·CBM 데이터 기초 공동설계 — 킥오프 핸드오프

**품질혁신파트(안나) × 물류파트 공식 협업 · 2026-06-24 킥오프**

> ⚠️ **DB 백엔드 잠정:** 신규 SSOT의 DB 백엔드(Supabase 등)는 **아직 미확정**. 본 문서의 플랫폼 관련 내용은 전부 **잠정(TBD)** — 킥오프 이후 안나 주도 공동 결정 대상. 확정된 원칙은 "운영 데이터는 백엔드 선택과 무관하게 Airtable 유지" 하나뿐.

---

## 1. 결론 먼저 — 무엇을 하고 싶은가

> **"프로젝트 등록 → 기사 운임마감까지 자동으로 흐르는 물류 기준 ERP"를 만든다.**

그러려면 **맨 밑바닥 — 아이템 정체성 SSOT — 부터 새로 기초공사를 시작**해야 한다.
그 위에 순서대로 쌓는다:

```
①정체성 SSOT → ②계층 → ③BOM(소요량) → ④CBM(부피) → ⑤수량전파(cascade)
```

- 물류가 혼자 테스트로 만든 **cascade는 "이 최종 흐름이 실제로 가능하다"를 보여주는 증명 예시(프로토타입)** 일 뿐이다. 데이터 기초는 **안나 주도로 새로** 세운다.
- 이 킥오프의 핵심 질문 = **"어디서부터 SSOT를 잡고, 데이터 구조를 짜고, BOM·CBM을 쌓을 것인가"** → 5단계 기초공사 순서(§4)로 답한다.

**오늘 반드시 닫을 것 → 로드맵 순서(§7) + RACI(§6).**

---

## 2. 배경 — 왜 지금인가

경영진이 **BOM·CBM 공식 협업 + 안나-물류파트 공식 협업**을 최종 승인했다.

현재 구조는 `SERPA 3.0 → sync → WMS/TMS(Airtable)` 위에서 6개 모듈이 돌아간다:

```
project(고객주문) → order → task(생산·임가공) → pkg(패키징) → movement(자재투입) → TMS(배송)
```

이 6개 모듈은 Airtable 네이티브 링크가 아니라 **텍스트 키(프로젝트코드·아이템코드·파츠코드)로만 위태롭게** 이어져 있다. 그 결과:

| 문제 | 현상 |
|---|---|
| **BOM 마스터 부재** | 구조화된 자재명세 테이블 0개. 사람이 프로젝트마다 BOM 로직을 머릿속으로 재실행. |
| **CBM 마스터 반쪽** | 신뢰 가능한 부피 숫자가 TMS Product에만. WMS/MES는 텍스트 규격 → 부피 계산 불가. |
| **정체성 약점** | 굿즈명 텍스트 조인 성공률 **53%** — 이름이 조금만 달라도 연결이 끊긴다. |

→ **정체성·계층(무엇인가)** 이 안나 도메인, **BOM·CBM·전파(얼마나·어떤 부피로 흐르는가)** 가 물류 도메인. 기초부터 함께 세울 최적 타이밍.

---

## 3. 최종 비전 — cascade 예시로 (완성되면 이렇게 된다)

한 프로젝트가 등록되는 순간 아래가 **자동으로** 흐른다. (물류 프로토타입이 이미 시연 — 아래는 실제 dry-run 산출값)

```
프로젝트 등록  PNA50702 · 심볼아크릴트로피 125개
     │
     ▼  ①정체성+②계층+③BOM 있어야 가능
BOM 폭발      구성 파츠 3종 (PT4900 · PT4906 · PT4917) × 125
     │
     ▼  재고 연결
부족 자재 산출  재고 0 → 소요 125씩
     │
     ▼  ④CBM 있어야 부피 획득
CBM 계산      입하 0.009 m³ · 출하 1.81 m³
     │
     ▼  부피 → 적재
배차 판정      W1/W2/W3 트럭 전부 적재 가능 (1.81 ≤ 4.5/7.6/9.5)
     │
     ▼  거리 → 운임마감
운임 프리뷰    ₩64,000 ~ ₩105,500 (수도권 거리대)
     │
     ▼
전파원장 1행   order→운임 무손실 추적 (INSERT-only)
```

> **핵심 통찰:** 이 흐름이 지금 **"부분(부족)"** 으로 도는 이유 = **밑바닥 데이터 기초가 없어서**다.
> 위 예시도 파츠 3종 중 **2종(PT4900·PT4906)은 CBM 미등록**이라 입하 부피가 비어 있다.
> → **기초(정체성·계층·CBM)를 세우면 이 그림이 "완결"로 돈다.** 이게 우리가 함께 만들 목표.

---

## 4. 어디서부터? — 기초공사 5단계 (SSOT-first) ★핵심

밑에서 위로 순서대로 쌓는다. 각 층은 아래 층이 있어야 성립한다.

| 단계 | 무엇 | 왜 여기부터 | 주도 | 위치(잠정) |
|---|---|---|:---:|---|
| **L0 · 아이템 정체성 SSOT** | 하나의 품목 = 하나의 표준 정체성. 원자 단위 2종(굿즈·PT####). **코드 정합: 굿즈코드 ↔ PT#### ↔ 견적코드 ↔ SERPA → 1개 표준키** | 모든 것의 뿌리. 없으면 굿즈명 조인 53% 약점 발생 | **안나** | 신규 SSOT *(DB 백엔드 미정·잠정)* |
| **L1 · 아이템 계층** | 완제품(굿즈) → 키트 → 구성품 → 부자재 → 포장재. 품목유형 분류(산출물·서비스 PT 구분 포함) | 정체성 위 부모-자식 골격 | **안나** | 신규 SSOT *(DB 백엔드 미정·잠정)* |
| **L2 · BOM (소요량)** | 모품목 → 소품목 + 소요량_개당. order 그룹핑 부트스트랩 → 검증 승급 | 계층 엣지에 수량을 매김 | **물류** | Airtable (운영) |
| **L3 · CBM (부피)** | 각 아이템에 부피 속성. 굿즈 CBM(박스규격) · 파츠 CBM(치수) | 아이템 정체성에 부피 바인딩 | **물류** | Airtable (운영) |
| **L4 · 수량전파 (cascade)** | 등록 → 폭발 → 소요 → CBM → 배차 → 운임마감 자동 흐름 | L0~L3 완비 시 §3 비전 완결 | **물류** | Airtable (운영) |

---

## 5. SSOT 경계 & 소유 — 계층형 소유 + 계약 심(seam)

한쪽이 전부 소유하지 않는다. 상위(정체성)와 하위(운영)를 명확한 인터페이스 계약으로 잇는다.

```
[안나 / 품질혁신] 상위 마스터            [물류] 하위 운영전파
아이템 정체성 · 계층 SSOT     ── 계약 ──▶  BOM 소요량 + CBM + 수량전파
(신규 SSOT · 백엔드 잠정)     (표준키       (Airtable 유지 — 운영데이터 정책)
 · 품목 정체성 / 명명 규칙     crosswalk)   · 소요량_개당 (BOM qty-per)
 · 계층 (완제품→…→포장재)                   · CBM 실측 / 추정
 · 코드 정합 (4종 → 1 표준키)                · order→자재→CBM→배차→운임 전파
```

- **안나(상위) 소유:** 품목이 **"무엇인가"** — 정체성·계층·코드 정합.
- **물류(하위) 소유:** 품목이 **"얼마나·어떤 부피로 흐르는가"** — BOM·CBM·전파. 운영 데이터라 **Airtable 유지** *(CLAUDE.md 정책: WMS/TMS 운영데이터는 Airtable 단일 원천, 외부 DB 이중화 금지 — 백엔드 선택과 무관하게 확정)*.
- **계약 심(seam):** 표준키 crosswalk. 안나가 표준 정체성+계층+코드를 발행 → 물류가 소비(53% 약점 → 표준코드로 해소) → 물류가 모은 BOM 부트스트랩·CBM 추정 로직·cascade 파이프라인을 seed로 역제공.

**물류 프로토타입의 위치:** 폐기 아닌 **참고 자산 + 시드**. "우리 것에 맞춰라"가 아니라 **"기초는 새로, 교훈은 활용"**.

---

## 6. 이해관계자 & RACI ★오늘 확정

> **A** = 최종책임 · **R** = 실행 · **C** = 협의 · **I** = 공유. 스폰서·추가 참석자는 `[확인]`.

| 작업 | 안나 (품질혁신) | 물류 (윤지수) | 스폰서 / IT |
|---|:---:|:---:|:---:|
| L0 아이템 정체성 SSOT 설계 | **A / R** | C *(프로토타입 crosswalk seed)* | I |
| 코드 정합 (굿즈코드↔PT####↔견적코드↔SERPA) | **A** | R | C *(SERPA/IT)* |
| L1 계층 · 품목유형 분류 | **A / R** | C | I |
| L2 BOM 소요량 구축 · 검증 | C | **A / R** | I |
| L3 CBM (굿즈 · 파츠) 구축 | I | **A / R** | I |
| L4 수량전파 cascade 재구축 | I | **A / R** | I |
| 표준키 crosswalk 계약 (인터페이스) | **A** | R | C |
| 스냅샷 대시보드 *(백엔드 잠정)* | C | **R** | `[확인]` |

---

## 7. 로드맵 순서 ★오늘 확정

기초공사 순서(L0→L4)를 그대로 일정화. 기존 물류 7~12월 작업(Chain)을 흡수한다.

| Phase | 시기 | 핵심 | 주도 |
|---|---|---|:---:|
| **P0 · 킥오프 · 정렬** | 7월 W1 (오늘) | 비전(cascade 예시) 공유, 기초 5단계·SSOT 경계 원칙 합의, **로드맵+RACI 확정** | 공동 |
| **P1 · L0+L1 정체성·계층** | 7~8월 | 안나 주도 정체성 SSOT + 계층 + 코드 4종 정합. 물류가 crosswalk/BOM/CBM 시드·요구사항 제공 | 안나 |
| **P2 · L2 BOM** | 8~9월 | 계층 위 BOM 소요량 구축 + 검증 | 물류 |
| **P3 · L3 CBM** | 9~10월 | 아이템에 CBM 바인딩 (파츠 CBM 등록 포함) | 물류 |
| **P4 · L4 전파·통합** | 11~12월 | 신 기초 위 project→운임 전체 흐름 재구축 + 스냅샷 대시보드 *(백엔드 잠정)* + 연말 리뷰 · 2027 스코프 | 공동 |

---

## 8. 스코프 (In / Out)

| ✅ In Scope | ❌ Out of Scope |
|---|---|
| L0~L4 데이터 기초 (정체성·계층·BOM·CBM·전파) | SERPA 3.0 원본 수정 (read-only) |
| 코드 체계 4종 정합 (1 표준키) | 운영 데이터(movement·inventory)의 외부 DB 이중화 |
| 표준키 crosswalk 계약 | TMS 운임/배차 엔진 재작성 |
| 물류 프로토타입 seed 이관 | *(신규 SSOT DB 백엔드 선택 — 별도 공동 결정, 잠정)* |

---

## 9. 리스크 · 가정 · 의존성

| 항목 | 내용 | 대응 |
|---|---|---|
| **기초 재구축 규모** | 밑바닥부터 = 큰 작업 | 물류 프로토타입을 폐기 아닌 시드로 → 기간 단축 |
| **SSOT DB 백엔드 미확정** | Supabase 여부 포함 잠정 | 안나 주도 공동 결정. 운영데이터는 백엔드 무관 Airtable 유지 |
| **코드 정합 난이도** | SERPA 원본 코드 접근·규칙 필요 | IT 협의 (RACI C) |
| **Immutable Ledger** | movement·전파원장 INSERT-only | 정정은 역분개(Storno)/보정 레코드 |
| **범위 경계** | SERPA write 금지, TMS 엔진 무변경 | Out of Scope 명시 |

---

## 10. 의사결정 포인트 & 다음 단계

**오늘 킥오프에서 닫을 결정:**
1. ☐ 기초공사 5단계(L0→L4) 순서 합의
2. ☐ SSOT 경계 원칙 합의 (정체성·계층=안나 / 운영전파=물류·Airtable)
3. ☐ **로드맵 순서(P0~P4) 확정**
4. ☐ **RACI 확정** (담당·협의·공유)

**다음 단계 (킥오프 직후):**
- 안나: L0 정체성 SSOT 초안 설계 착수 (물류 crosswalk seed 수령)
- 물류: 프로토타입 데이터·로직 시드 패키징 + BOM/CBM 요구사항 정리

---

## 부록 — 오늘 확정 필요 `[확인]` 항목

- 스폰서 · 전체 참석자 명단
- **SSOT DB 백엔드** (Supabase 여부 — 현재 잠정)
- 킥오프 시간 · 장소
- 물류 프로토타입 데이터의 시드 활용 범위

---

*작성: 물류파트 윤지수 · 2026-06-24 · 데이터 출처: 물류 cascade dry-run 실측(`data/order_cascade/`), BOM·CBM 백본 스펙*
