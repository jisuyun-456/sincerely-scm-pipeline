# Retool UI 레이아웃 가이드 — 10페이지 배치도

> ⚠️ **Deprecated** — 이 문서는 `docs/retool/retool_master.md`로 통합되었습니다.

> 각 페이지의 컴포넌트 배치를 ASCII 와이어프레임으로 표현.
> Retool 에디터에서 이 가이드를 보면서 컴포넌트를 드래그&드롭으로 배치하세요.

---

## Page 1: Dashboard

```
┌─────────────────────────────────────────────────────────┐
│  [Stat] 총 프로젝트   [Stat] 진행중   [Stat] 완료   [Stat] 이번달 배송  │
│    25                  20              5              1                │
├─────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌─ tbl_projects ──────────────────────────────────────────────┐     │
│  │ 프로젝트코드 │ 프로젝트명 │ 고객사 │ 상태 │ 담당 │ 첫출고일 │ 리드타임 │     │
│  │ PRJ-025     │ 프리비알.. │ 프리비알│active│ 우예림│ Apr 1   │ 7      │     │
│  │ PRJ-024     │ 올릿 사무..│ 올릿  │active│ 현동원│         │        │     │
│  │ ...         │           │       │      │      │         │        │     │
│  └──────────────────────────────────────── Showing 1-20 of 25 ┘     │
│                                                                       │
│  ┌─ status_select ─┐  ┌─ 상태 변경 ─┐                                │
│  │ planning ▼      │  │   [Button]  │                                │
│  └─────────────────┘  └────────────┘                                │
└─────────────────────────────────────────────────────────┘
```

**컴포넌트 목록:**
- Statistic x4: `statistic1`(총), `statistic2`(진행중), `statistic3`(완료), `statistic4`(배송)
- Table: `tbl_projects` (Data: q_projects_list) — 행 클릭 → project_detail
- Select: `select1` (status_select, Manual: planning/active/completed/on_hold)
- Button: `button1` (상태 변경 → q_project_update_status)

---

## Page 2: Project Detail

```
┌─────────────────────────────────────────────────────────┐
│  ### PRJ-2026-025 · 프리비알 VIP 이벤트 150세트                       │
│  고객: 프리비알 | 상태: active | 담당: 우예림                          │
├─────────────────────────────────────────────────────────┤
│  ┌─ select_project ──────────────────┐                               │
│  │ PRJ-2026-025 · 프리비알 VIP... ▼  │                               │
│  └───────────────────────────────────┘                               │
├─────────────────────────────────────────────────────────┤
│  ┌─ tabbedContainer1 ──────────────────────────────────────────┐    │
│  │ [ 발주 ] [ 입고 ] [ 생산 ] [ 배송 ]                          │    │
│  │                                                              │    │
│  │  ┌─ table1 (발주) ────────────────────────────────────┐     │    │
│  │  │ PO번호 │ 공급업체 │ 상태 │ 요청일 │ 품목명 │ 단가 │ 총액 │     │    │
│  │  │ ...    │         │      │       │       │     │     │     │    │
│  │  └────────────────────────────────────────────────┘     │    │
│  └──────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
```

**컴포넌트 목록:**
- Text: `text1` (프로젝트 헤더 — q_project_info 바인딩)
- Select: `select_project` (Mapped, Data: q_all_projects)
- TabbedContainer: `tabbedContainer1` (4탭: 발주/입고/생산/배송)
  - Tab 1 (발주): `table1` (Data: q_project_pos)
  - Tab 2 (입고): `table2` (Data: q_project_grs)
  - Tab 3 (생산): `table3` (Data: q_project_production)
  - Tab 4 (배송): `table4` (Data: q_project_delivery)

---

## Page 3: Purchase Orders (구매/발주)

```
┌─────────────────────────────────────────────────────────┐
│  # 구매/발주                                                          │
├──────────────────────────┬──────────────────────────────┤
│  ┌─ tbl_po (좌측) ──────┐│  ┌─ tbl_po_items (우측) ───┐│
│  │ PO번호  │ 프로젝트코드 ││  │ 라인 │ 품목코드 │ 품목명  ││
│  │ 프로젝트명│ 공급업체    ││  │ 발주수량│ 입고수량│ 단가   ││
│  │ 상태    │ 요청일      ││  │ 총액  │ 검사결과│        ││
│  │ 총액    │             ││  │      │        │        ││
│  │                      ││  │                         ││
│  │  PO 행 클릭 →        ││  │  ← 선택된 PO의 품목 표시  ││
│  │                      ││  │                         ││
│  └──────────────────────┘│  └─────────────────────────┘│
│  Showing 1-20 of 32      │  Showing 1-3 of 3           │
└──────────────────────────┴──────────────────────────────┘
```

**컴포넌트 목록:**
- Text: `purchaseOrdersTitle` (# 구매/발주)
- Table (좌): `tbl_po` (Data: q_po_list, Primary key: id, 6:6 비율)
- Table (우): `tbl_po_items` (Data: q_po_items, 6:6 비율)
- tbl_po Event handler: Select row → q_po_items trigger

---

## Page 4: Goods Receipt (입고/검수)

```
┌─────────────────────────────────────────────────────────┐
│  # 입고/검수                                                          │
├─────────────────────────────────────────────────────────┤
│  ┌─ tabbedContainer2 ──────────────────────────────────────────┐    │
│  │ [ 입고내역 ] [ 입고대기 ] [ 반품 ]                            │    │
│  │                                                              │    │
│  │  ┌─ tbl_gr_list ──────────────────────────────────────┐     │    │
│  │  │ GR번호 │ PO번호 │ 품목명 │ 입고수량 │ 합격 │ 불합격 │ 단가 │     │    │
│  │  │ 총액   │ 입고일 │ 검사결과│         │     │       │     │     │    │
│  │  └────────────────────────────────────────────────┘     │    │
│  └──────────────────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────────┤
│  ┌─ GR 등록 Form ──────────────────────────────────────────────┐    │
│  │ [PO선택▼] [품목선택▼] [입고수량] [합격수량] [불량수량]          │    │
│  │ [단가]    [입고일]    [세금계산서] [검수결과▼]                  │    │
│  │                                          [ 입고 등록 ]       │    │
│  └──────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
```

**컴포넌트 목록:**
- Text: `goodsReceiptTitle` (# 입고/검수)
- TabbedContainer: `tabbedContainer2` (3탭: 입고내역/입고대기/반품)
  - Tab 1: `tbl_gr_list` (Data: q_gr_list)
  - Tab 2: `tbl_pending_receipt` (Data: q_pending_receipt)
  - Tab 3: `tbl_returns` (Data: q_returns)
- Form (하단):
  - Select: `gr_po_select`, `gr_parts_id`, `gr_inspection_result`
  - NumberInput: `gr_received_qty`, `gr_accepted_qty`, `gr_rejected_qty`, `gr_unit_cost`
  - TextInput: `gr_receipt_date`, `gr_tax_invoice`
  - Button: 입고 등록 → q_gr_insert

---

## Page 5: Inventory (창고/재고)

```
┌─────────────────────────────────────────────────────────┐
│  # 창고/재고                                                          │
│  재고 정상 (또는 "음수 재고 N건 발견!")                                │
├─────────────────────────────────────────────────────────┤
│  ┌─ tbl_inventory ─────────────────────────────────────────────┐    │
│  │ 품목코드│ 품목명 │ 창고 │ BIN │ 존 │ 재고유형│ 실재고│ 예약│ 가용 │    │
│  │ 배치   │ 단가  │ 재고금액│    │    │        │      │    │     │    │
│  └──────────────────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────────┤
│  ┌─ tbl_stock_movements ───────────────────────────────────────┐    │
│  │ 이동번호│ 유형코드│ 유형 │ 품목명│ 수량│ 단가│ 전기일│ 상태│ 취소 │    │
│  └──────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
```

**컴포넌트 목록:**
- Text: `inventoryTitle` (# 창고/재고)
- Text: `negativeStockWarningText` (음수 재고 경고)
- Table (상): `tbl_inventory` (Data: q_inventory)
- Table (하): `tbl_stock_movements` (Data: q_stock_movements)

---

## Page 6: Production (생산/조립)

```
┌─────────────────────────────────────────────────────────┐
│  # 생산/조립                                                          │
├──────────────────────────┬──────────────────────────────┤
│  ┌─ tbl_production ─────┐│  ┌─ tbl_bom_components ────┐│
│  │ 오더번호│ 프로젝트코드 ││  │ 부품코드│ 부품명        ││
│  │ 상태   │ 프로젝트명   ││  │ 단위소요│ 총소요│ 불출   ││
│  │ 계획시작│ 계획종료    ││  │ 잔량   │ 현재고│        ││
│  │ 실시작 │ 실종료      ││  │        │      │        ││
│  │ 계획수량│ 실수량      ││  │                         ││
│  │ 산출수량│ 진행률(%)   ││  │  ← 선택된 오더의 BOM     ││
│  │                      ││  │                         ││
│  └──────────────────────┘│  └─────────────────────────┘│
├──────────────────────────┴──────────────────────────────┤
│  ┌─ prod_status_select ─┐  ┌─ 상태 변경 ─┐                          │
│  │ in_progress ▼        │  │   [Button]  │                          │
│  └──────────────────────┘  └────────────┘                          │
└─────────────────────────────────────────────────────────┘
```

**컴포넌트 목록:**
- Text: `productionTitle` (# 생산/조립)
- Table (좌): `tbl_production` (Data: q_production_orders, 6:6)
- Table (우): `tbl_bom_components` (Data: q_bom_components, 6:6)
- Select: `prod_status_select` (Manual: planned/in_progress/completed/cancelled)
- Button: 상태 변경 → q_production_update_status

---

## Page 7: Delivery (배송/물류)

```
┌─────────────────────────────────────────────────────────┐
│  # 배송/물류                                                          │
├─────────────────────────────────────────────────────────┤
│  ┌─ tbl_transport_requests ────────────────────────────────────┐    │
│  │ TR번호│ 프로젝트코드│ 프로젝트명│ 배송유형│ 수취인│ 주소│ 요청출고일│ 상태│  │
│  └──────────────────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────────┤
│  ┌─ tbl_freight_orders ────────────────────────────────────────┐    │
│  │ FO번호│ TR번호│ 운송사│ 유형│ 계획출고│ 확정출고│ 출발│ 도착│ 배송상태│  │
│  │ CBM  │ 운임  │ Tracking│    │        │        │    │    │        │  │
│  └──────────────────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────────┤
│  ┌─ 운임 청구 처리 ─┐                                                │
│  │    [Button]      │                                                │
│  └──────────────────┘                                                │
└─────────────────────────────────────────────────────────┘
```

**컴포넌트 목록:**
- Text: `deliveryTitle` (# 배송/물류)
- Table (상): `tbl_transport_requests` (Data: q_transport_requests)
- Table (하): `tbl_freight_orders` (Data: q_freight_orders)
- Button: 운임 청구 처리 → q_freight_mark_billed

---

## Page 8: Finance (회계/전표)

```
┌─────────────────────────────────────────────────────────┐
│  # 회계/전표                                                          │
├─────────────────────────────────────────────────────────┤
│  ┌─ tbl_journal_entries ───────────────────────────────────────┐    │
│  │ 전표번호│ 전표일자│ 유형│ 차변│ 대변│ 금액│ 상태│ 역분개│ 더존전표│    │
│  └──────────────────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────────┤
│  ┌─ slip_no_input ──┐                                                │
│  │ 더존 전표번호     │                                                │
│  └──────────────────┘                                                │
│  ┌─ 검토 완료 ─┐  ┌─ 더존 확정 ─┐  ┌─ 검토 취소 ─┐                   │
│  │  [Button]   │  │  [Button]   │  │  [Button]   │                   │
│  └─────────────┘  └─────────────┘  └─────────────┘                   │
├─────────────────────────────────────────────────────────┤
│  ┌─ tbl_period_closes ─────────────────────────────────────────┐    │
│  │ 기간│ 품목코드│ 품목명│ 창고│ 마감수량│ 단가│ 마감금액│ 마감여부│       │
│  └──────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
```

**컴포넌트 목록:**
- Text: `financeTitle` (# 회계/전표)
- Table (상): `tbl_journal_entries` (Data: q_journal_entries)
- TextInput: `slip_no_input` (더존 전표번호)
- Button x3: 검토완료(q_entry_mark_reviewed), 더존확정(q_entry_post_douzone), 검토취소(q_entry_revert_draft)
- Table (하): `tbl_period_closes` (Data: q_period_closes)

---

## Page 9: Master Data (마스터 데이터)

```
┌─────────────────────────────────────────────────────────┐
│  # 마스터 데이터                                                      │
├─────────────────────────────────────────────────────────┤
│  ┌─ tabbedContainer3 ──────────────────────────────────────────┐    │
│  │ [ 고객 ] [ 공급업체 ] [ 품목 ] [ BOM ] [ 창고 ]               │    │
│  │                                                              │    │
│  │  ┌─ tbl_clients (고객 탭) ────────────────────────────┐     │    │
│  │  │ 고객코드│ 회사명│ 사업자번호│ 담당자│ 이메일│ 전화│ 상태│     │    │
│  │  └────────────────────────────────────────────────┘     │    │
│  │                                                              │    │
│  │  tbl_vendors (공급업체 탭)                                    │    │
│  │  tbl_parts (품목 탭)                                         │    │
│  │  tbl_bom (BOM 탭)                                            │    │
│  │  tbl_warehouses (창고 탭)                                     │    │
│  └──────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
```

**컴포넌트 목록:**
- Text: `masterDataTitle` (# 마스터 데이터)
- TabbedContainer: `tabbedContainer3` (5탭)
  - Tab 1 (고객): `tbl_clients` (Data: q_clients)
  - Tab 2 (공급업체): `tbl_vendors` (Data: q_vendors)
  - Tab 3 (품목): `tbl_parts` (Data: q_parts)
  - Tab 4 (BOM): `tbl_bom` (Data: q_bom)
  - Tab 5 (창고): `tbl_warehouses` (Data: q_warehouses)

---

## Page 10: Workflow (워크플로우 추적기)

```
┌─────────────────────────────────────────────────────────┐
│  # 워크플로우 추적기                                                  │
├─────────────────────────────────────────────────────────┤
│  ┌─ project_selector ───────────────────┐                            │
│  │ PRJ-2026-025 — 프리비알 VIP... ▼     │                            │
│  └──────────────────────────────────────┘                            │
├─────────────────────────────────────────────────────────┤
│  ┌─ tbl_workflow ──────────────────────────────────────────────┐    │
│  │ 프로젝트코드│ 상태│ 첫출고일│ PO번호│ PO상태│ GR번호│ 입고일│        │
│  │ 생산오더   │ 생산상태│ FO번호│ 배송상태│ 운임│ 전표번호│ 전표유형│     │
│  │ 전표금액   │ 전표상태│       │        │     │        │        │     │
│  └──────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
```

**컴포넌트 목록:**
- Text: 워크플로우 추적기 제목
- Select: `project_selector` (Mapped, Data: q_util_project_dropdown)
- Table: `tbl_workflow` (Data: q_workflow_tracker)

---

## 전체 요약

| Page | 컴포넌트 수 | 쿼리 수 | 핵심 인터랙션 |
|------|-----------|---------|-------------|
| Dashboard | 7 | 5 | 행 클릭→project_detail, 상태변경 |
| Project Detail | 7 | 6 | 탭 전환, 드롭다운 프로젝트 선택 |
| Purchase Orders | 3 | 2 | PO 클릭→품목 표시 |
| Goods Receipt | 13 | 5 | 탭 전환, GR 등록 Form |
| Inventory | 4 | 3 | 음수 재고 경고 |
| Production | 5 | 2 | 오더 클릭→BOM, 상태변경 |
| Delivery | 4 | 2 | 운임 청구 버튼 |
| Finance | 7 | 2 | 전표 검토/확정/취소, 더존번호 |
| Master Data | 7 | 5 | 5탭 전환 |
| Workflow | 3 | 2 | 프로젝트 선택→전체 흐름 |
| **합계** | **~60** | **~34** | |
