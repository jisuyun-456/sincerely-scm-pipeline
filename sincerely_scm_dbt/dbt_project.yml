name: sincerely_scm_dbt
version: '1.0.0'
config-version: 2
profile: sincerely_scm_dbt

model-paths: ["models"]
test-paths: ["tests"]
snapshot-paths: ["snapshots"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  sincerely_scm_dbt:
    staging:
      +schema: analytics
      +materialized: view
    marts:
      +schema: analytics
      +materialized: table
